# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Django / banco ──────────────────────────────────────────────────────────
db.sqlite3
*.sqlite3
staticfiles/
media/

# ── Ambiente ────────────────────────────────────────────────────────────────
.env
venv/
.venv/

# ── Sistema ─────────────────────────────────────────────────────────────────
.DS_Store
*.DS_Store
.~lock.*#

# ── Dados / artefatos locais (NÃO commitar) ─────────────────────────────────
chipid_data/
prod_data.json
# rede de regressão (characterize_baseline) — comentário em linha própria:
# o .gitignore NÃO aceita comentário inline (viraria parte do padrão e não casa nada)
baseline*.json

# Reverts e backups dos comandos dry-run/--revert (Problema 4.3c: nunca na raiz)
*_revert.json
*_revert.json.*
*.bak
*.done
purge_enriched_backup_*.json

# Planilhas de trabalho soltas na raiz
/WTC_*.xlsx
/precificacao_*.xlsx

# Artefatos de sessão (T1-T3, jul/2026)
.~*
/resumo_*.xlsx
correct_kp_revert_*.json
