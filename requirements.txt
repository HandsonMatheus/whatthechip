# Core Django
Django==5.2.15          # LTS; fixado p/ paridade local↔Render. NÃO subir p/ 6.0 (exige Python 3.12+; runtime é 3.11)
Pillow>=10.0
psycopg2-binary>=2.9
django-pghistory>=3.9,<4  # auditoria do catálogo (passo 3); puxa django-pgtrigger (gatilhos Postgres)

# Conhecimento por marca em YAML (passo 4): load_brands lê os .yaml e valida com Pydantic
PyYAML>=6.0
pydantic>=2,<3

# Produção / deploy
whitenoise>=6.5
dj-database-url>=2.1

# Variáveis de ambiente
python-dotenv>=1.0

# Scraping — coleta de Part Numbers
curl_cffi>=0.7          # TLS fingerprint impersonation (bypass bot detection)
playwright>=1.40        # Browser real para Cloudflare JS challenge
                        # Após instalar: playwright install chromium

# Utilitários dos scripts
tqdm>=4.0               # Barra de progresso no enrich_gemini.py
openpyxl>=3.1           # Exportação de estoque para .xlsx

# Coleta FBGA — pipeline bulk (stages 2-5)
pdfplumber>=0.10        # Extração de tabelas de datasheets PDF (Stage 2)
requests>=2.31          # Fallback HTTP simples (Wayback CDX API, etc.)
